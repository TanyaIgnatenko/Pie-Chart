import React, { FC } from 'react';

type TPieChartProps = {};

const PieChart: FC<TPieChartProps> = ({}) => {

    return (
        <div>
            Pie Chart
        </div>
    );
};

export default PieChart;
